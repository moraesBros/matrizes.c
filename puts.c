#include <stdio.h>

int main() {
    char mensagem[] = "Bem-vindo ao programa!";

    // Exibindo a mensagem com puts()
    puts(mensagem);

    // Exemplo de uso direto da string
    puts("Este é um exemplo prático do uso de puts().");

    return 0;
}
